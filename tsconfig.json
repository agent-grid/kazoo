{
  // Solution file — references only. `npm run typecheck` (`tsc -b`) walks
  // both leaf projects. One config can't serve the DOM-less main/core and the
  // DOM renderer, so they're split: node (main + preload + core + shared) and
  // web (renderer + shared).
  "files": [],
  "references": [{ "path": "./tsconfig.node.json" }, { "path": "./tsconfig.web.json" }]
}
